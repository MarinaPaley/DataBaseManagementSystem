#include <iostream>
#include <algorithm>
#include "..\Vector\Vectorr.h"


int main()
{
    dbms::Vector v1{ 0, 1, 2 };
    dbms::Vector v2{ 3, 4, 5 };
    dbms::Vector v3{ 6, 7, 8 };
    std::cout << v.ToString();


    auto v = new vector<dbms::Vector>;
    v.push_back(v1);
    v.push_back(v2);
    v.push_back(v3);

    for(auto item in v )
    {
        std::cout << v << "\n";
    }
    return 0;
}